{
    "demands": [{
        "demandingName": "gpii.requests.request.handler",
        "contextNames": "pcpUpdate",
        "demandSpec": {
            "options": {
                "invokers": {
                    "handle": {
                        "funcName": "gpii.requests.request.handler.pcpUpdate",
                        "args": ["{requestProxy}", "{request}", "{flowManager}.personalControlPanel.userToken", "{flowManager}.personalControlPanel"]
                    }
                }
            }
        }
    }]
}
