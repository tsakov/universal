(function () {

    "use strict";

    var fluid = require("infusion"),
        gpii = fluid.registerNamespace("gpii");

    gpii.requests.request.handler.pcpMain = function(requestProxy) {
		requestProxy.events.onSuccess.fire("main handler\n");
		
		//console.log("main handler");
		//require("child_process").exec("firefox");
	};

})();